# 《AI电投助手》后端架构设计文档

> 版本：v0.1（评审稿）
> 来源：基于《AI电投助手_产品说明书》及后端团队评审结论整理
> 状态：待评审，各模块后续逐步细化

---

## 1. 产品定位与后端边界

### 1.1 产品一句话

面向电网、电力设备制造及电力工程企业的投标工作台：以一次投标项目为核心，统一管理企业资料、当前招标材料、商务标/技术标/报价单三份成果、模拟评标、提升建议和历史报价，覆盖从材料上传到成果导出的完整投标流程。

### 1.2 四方分工

| 角色 | 职责 |
|---|---|
| 前端 | 页面展示、类 Word/Excel 在线编辑、选区操作、版本切换、差异呈现、进度与消息展示 |
| **后端业务服务（本团队）** | 认证、数据隔离、项目/资料/成果/版本/评分/报价/审计等业务 API；文件解析与存储；任务编排；导出转换；确定性算法 |
| Hermes Agent | AI 大脑：招标解析、资料匹配、标书生成/校核、模拟评标、针对性修改、对话问答 |
| 独立算法服务 | 历史报价库、单价测算、报价策略（**由本团队以后端模块形式提供**，非第三方） |

### 1.3 必须遵守的产品约束（来自说明书）

1. 一个账号对应一个唯一企业主体，所有数据按企业隔离
2. 当前招标材料为必传项；无已有标书 → 开始生成，有任意已有标书 → 开始校核
3. 资料不足可继续处理，但必须显示缺失项和风险；**禁止模型编造企业事实**
4. 原始上传文件只读保存，AI 修改和用户修改均形成新版本
5. 招标文件解析出的要求优先于系统内置评审规则
6. 每个评分项、风险、提升建议必须能定位到招标要求 / 成果内容 / 企业材料
7. 商务标、技术标、报价单必须保持项目名称、企业主体、金额、工期、参数、数量、报价一致
8. 智能报价只提供建议，用户确认后才能写入报价单
9. 系统不自动登录或提交官方投标平台，最终提交由企业授权人员完成

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│ 前端（Web）                                                  │
│  工作台 / 资料库 / 在线编辑器 / 生成进度 / 评分建议 / 报价    │
└──────────────────────┬──────────────────────────────────────┘
                       │ HTTPS / WebSocket(SSE)
┌──────────────────────▼──────────────────────────────────────┐
│ 后端业务服务（BidVolt API，中枢）                              │
│ ┌────────────┐ ┌──────────────┐ ┌─────────────────────────┐  │
│ │认证/数据隔离│ │项目/资料/成果 │ │文件服务                  │  │
│ │ 会话/编辑锁 │ │ 版本/审计     │ │ 上传·解包·解析·不可变存储 │  │
│ └────────────┘ └──────────────┘ └─────────────────────────┘  │
│ ┌────────────────┐ ┌────────────┐ ┌───────────────────────┐  │
│ │ 文档结构化模型   │ │ 报价算法服务 │ │ 导出转换               │  │
│ │ (JSON 模型+版本) │ │ (确定性计算) │ │ DOCX/XLSX             │  │
│ └────────────────┘ └────────────┘ └───────────────────────┘  │
│ ┌─────────────────────────────────────────────────────────┐  │
│ │ 任务编排（异步队列 + 进度/消息 SSE 推送）                  │  │
│ └─────────────────────────────────────────────────────────┘  │
└───────────────┬──────────────────────┬───────────────────────┘
                │ 任务提交 / 消息回调    │ MCP（业务能力）
┌───────────────▼──────────────────────▼───────────────────────┐
│ Hermes Agent（独立服务，Profile + Session 隔离）               │
│  Skill：招标解析 / 资料匹配 / 标书生成校核 / 模拟评标 / 针对性修改 │
│  MCP：业务服务能力 · AnySearch 搜索 · 报价算法                  │
└──────────────────────────────────────────────────────────────┘
```

---

## 3. 核心领域模型（初稿）

| 领域对象 | 关键字段 | 后端要点 |
|---|---|---|
| 企业 Enterprise | id、名称、统一社会信用代码 | 数据隔离的根 |
| 用户 User | 邮箱、密码哈希 | 注册免邮件验证 |
| 投标项目 Project | 名称、招标编号、截止日期、状态机 | 状态：草稿→处理中→部分完成→完成 |
| 资料文件 Asset | 归属（企业资料/当前招标材料）、格式、解析状态 | 不可变存储 + 版本 |
| 招标解析结果 Requirement | 基本信息/资格要求/评分细则/否决条款/技术要求/报价规则/材料清单 | 每项带定位坐标 |
| 成果文件 Deliverable | 商务标/技术标/报价单、版本链 | 结构化模型 + COW 版本 |
| 评分 Score / 评分项 ScoreItem | 综合/商务/技术/报价分、否决风险数、可提升分 | 评分项关联三要素：招标原文、成果内容、证明材料 |
| 提升建议 Suggestion | 6 类分类、风险等级、预计提升、处理状态 | 闭环：待处理→已处理 |
| 报价 Quote | 历史样本、测算结果、策略、来源（算法/AI） | 用户确认后落库，进版本和审计 |
| 搜索来源 SearchSource | URL、抓取时间、来源级别、引用锚点 | 搜索引用的追溯基础 |
| 版本记录 Version | 版本号、类型（原始/AI生成/AI校核/用户修改）、内容指针 | 全量可追溯 |
| 操作审计 AuditLog | 操作人、动作、对象、基于版本 | 报价写入门户等关键动作必记 |

---

## 4. 系统模块详设

### 4.1 认证与数据隔离

- **登录**：邮箱 + 密码，注册不发送邮件验证；密码哈希存储（bcrypt/argon2），会话令牌（JWT 或服务端 session）
- **隔离**：所有业务表携带 `enterprise_id`，查询层强制过滤，企业间数据硬隔离
- **项目级编辑锁**：不支持多用户对同一项目并发编辑。进入编辑加锁、保存释放、超时自动释放；锁冲突提示"他人正在编辑"。为将来协作预留字段，不引入 OT/CRDT

### 4.2 文件服务

- **上传**：支持批量、文件夹、拖拽、压缩包（ZIP/RAR/7Z）；压缩包自动解包（注意 zip 炸弹防护、病毒扫描）
- **格式**：至少 PDF、OFD、DOC、DOCX、XLS、XLSX、CSV、PPT、PPTX、TXT、JPG/JPEG/PNG/BMP/TIFF、ZIP/RAR/7Z
- **存储**：原始文件不可变存储（对象存储/文件系统），AI 和用户修改写新对象（Copy-on-Write）
- **异常处理**：损坏文件、加密文件、无法解析文件、识别置信度低 → 明确提示用户处理，不静默丢弃

### 4.3 文档解析与坐标定位（混合架构）

- **提取层（确定性，用成熟 SDK，不自研）**——文字版文档直接读，不经过视觉模型：
  - 文本类 PDF → PyMuPDF / pdfplumber（有文本层，直接提取）
  - DOCX → python-docx；XLSX → openpyxl
  - OFD → **开源 OFDRW**（已确定采用）提取文本层
- **理解层（LLM/Hermes）**：对提取出的文本/表格做语义识别——判断文件类型（招标公告/技术规范书/评分办法/报价模板…），抽取资格要求、评分细则、否决条款、报价规则等结构化字段
- **视觉模型（qwen-vl）**：仅用于"无文本层"的内容，**不引入传统 OCR**：
  - 扫描版 PDF（纯图片，无文本层）
  - 图片文件（证照、盖章文件、图纸、工程照片）
  - DOCX 内嵌图片（需要理解内容时）
  - 扫描版 OFD
  - qwen-vl 本身同时具备 OCR + 语义理解能力，直接替代 PaddleOCR/Tesseract，无需本地 OCR 服务
- **部署**：qwen-vl 默认走云 API（DashScope），通过 Hermes 配置 `vision_model` 接入，无需本地部署；若后续有数据安全/私有化要求，再评估 vLLM + 开源 VL 模型本地部署
- **坐标体系**：提取层保留"文件 + 页码 + 段落/表格/单元格"索引；LLM/视觉模型抽取结果回写时**必须绑定坐标**，这是"每项识别结果可定位到原文"的技术基础

### 4.4 Hermes Agent 集成

- **部署形态**：独立服务（容器），采用 Python 库嵌入或 API Server（OpenAI 兼容）方式由后端调用；不对前端直接开放
- **调用方式**：后端任务编排模块提交任务 → Hermes 执行 → 回调/SSE 返回结果与消息
- **隔离（重要）**：账号间数据必须隔离。用 **Profile（每企业一个，隔离记忆与技能沉淀）+ Session（每项目一个，隔离对话上下文）** 实现逻辑隔离；物理多实例作为未来扩展项
- **并发模型**：不支持多用户对同一项目并发操作。任务队列 + 排队；用户处理中补充材料可**插队触发重新规划**（符合说明书"重新评估任务计划"）
- **Skill 清单（初版 5 个）**：
  1. 招标解析——识别材料类型、抽取结构化要求
  2. 资料匹配——招标要求 vs 企业资料库，产出匹配/缺失清单
  3. 标书生成/校核——三份成果生成与一致性检查
  4. 模拟评标——评分细则 + 内置规则组合打分
  5. 针对性修改——按选区产出修改差异
  > 详细设计：SKILL.md 草稿见 `docs/hermes/skills/`，业务 MCP 工具契约见 `docs/hermes/bidvolt-mcp-tools.md`
- **MCP 连接器清单**：业务服务能力（查资料/读材料/写版本）、报价算法服务、AnySearch 搜索
- **进度与消息**：后端将 Hermes 实际回复的消息通过 **SSE/WebSocket 原样透传**给前端（生成中页面、底部对话），不做事件转义

### 4.5 文档结构化模型与版本

- **结构化模型**：docx/xlsx 解析为 JSON 结构（段落、表格、图片、行列、单元格），前端在线编辑的对象是结构化模型而非二进制文件
- **版本链**：每次 AI 修改、用户保存、应用报价策略均形成新版本
  - 保留策略：每类成果保留**最近 20 版** + **里程碑版本永久保留**（原始上传 / AI 生成 / AI 校核 / 用户手动保存）
  - 存储：JSON 内容哈希去重，同内容不重复落盘
  - 恢复粒度：整文件/整版本切换，不做段落级恢复

### 4.6 在线编辑的后端职责

前端主导编辑交互，后端负责：
1. **文档内容服务**：结构化模型（JSON）的读取与保存
2. **版本服务**：保存即新版本（见 4.5）
3. **AI 针对性修改接口**：前端传"选区 + 上下文" → 后端调 Hermes → 返回修改差异 → 前端展示，用户确认后落版本
4. **导出转换服务**：结构化模型 → DOCX/XLSX 文件（后端统一实现，复用于成果下载）

### 4.7 报价能力（确定性算法 + AI 建议分层）

- **确定性算法服务**（后端普通模块，非 LLM，纯函数、可单测）：
  - 历史报价查询：按物料名称/编码/规格型号/招标人/地区/年份
  - 单价测算：历史中位数、近一年均价、同地区/同规格均价、时间/地区/规格调整、内部成本、最低毛利、投标上限、评分公式
  - 三类策略：中标优先 / 均衡报价 / 利润优先（同一优化问题的三个目标函数，解唯一确定）
- **AI 分层**：
  - 有明确评分公式 + 有历史数据 → 确定性测算，输出唯一建议价
  - 无评分公式或无可靠数据（EPC 总价、成本+利润自主定价等）→ AI 基于内部成本、毛利要求、市场信息生成**建议价（区间 + 理由 + 风险等级）**，标注"AI 策略建议"而非"测算结果"
- **AI 的角色**：AI 通过 MCP 调用算法服务取确定值，负责生成推荐理由与依据解释；**价格数值永远来自算法或 AI 建议并明确标注，不输出无来源的"确定报价"**
- **确认门**：报价必须用户确认后写入报价单，形成新版本 + 操作审计（AuditLog）

### 4.8 模拟评标与规则引擎

- **两类规则**：招标材料解析出的评分细则 + 系统内置评审规则库（否决风险、完整性、资料有效性、跨文件一致性、方案完整性、内容针对性、报价合理性、格式规范）
- **优先级**：招标要求优先，内置规则补充检查
- **输出**：综合/商务/技术/报价分、否决风险数、缺失材料数、预计可提升分值
- **证据链**：每个评分结论关联三要素（招标原文 / 成果内容 / 企业证明材料）＋ 可选第四类：搜索来源（带 URL）

### 4.9 网络搜索（AnySearch）

- **接入**：通过 **MCP** 接入 AnySearch，作为 Hermes 可选工具；Hermes 原生支持 MCP
- **默认开启**：写标书时按需检索行情 / 竞对 / 政策 / 行业标准，作为内容生产要素（市场分析、政策符合性等章节直接使用）
- **来源分级**：
  | 级别 | 处理 |
  |---|---|
  | 权威来源（政府网站、招标平台、官方公告、国家标准） | 可直接引用进正文，带来源标注 |
  | 一般来源（新闻、行业网站） | 可引用，带来源标注 |
  | 低可信来源（自媒体、论坛、不明页面） | AI 优先不引用，引用时明确提示用户核实 |
- **追溯**：搜索结果统一入库（URL、抓取时间、来源级别），Hermes 引用时结构化记录"哪段内容来自哪个来源"，前端展示出处，用户可点击核实
- **配额与降级**：优先从 Hermes 的 `.env` 读取 AnySearch API Key（注册额度 **每日 1000 次**，永久免费）；读不到则降级为匿名/无 Key 模式（限额较低，约每日 50 次，上线前实测校准）；做调用量配额监控，接近上限时提示或降级
- **边界**：搜索信息可进标书正文与评分依据（定位到来源即可）；招标材料本身仍以用户上传为准（必传项）

### 4.10 导出与终稿检查

- **一致性校验**：商务标/技术标/报价单的企业名称、项目名称、金额、工期、数量、参数、税率一致
- **检查项**：资质有效期、关键材料缺失、技术参数偏离、报价超限、计算错误、否决风险
- **导出**：商务标 DOCX、技术标 DOCX、报价单 XLSX（**可编辑格式**）＋ 三份成果 PDF（**成品格式**，终稿转换）；另提供模拟评分报告、材料清单、最终交付压缩包

---

## 5. 关键决策记录（ADR）

| # | 决策 | 结论 |
|---|---|---|
| D1 | Hermes 部署与调用 | 独立服务（Python 库/API Server），后端任务编排调用；不对前端开放 |
| D2 | Hermes 并发与隔离 | Profile（每企业）+ Session（每项目）逻辑隔离；任务队列 + 排队 + 轻量插队；不做多用户同项目并发 |
| D3 | 报价 AI/算法分工 | 能算的（有公式+数据）走确定性算法；算不了的（无公式）走 AI 建议并明确标注；数值不经 LLM 直接输出 |
| D4 | 版本策略 | 最近 20 版 + 里程碑永久保留；JSON 内容哈希去重；整版本级恢复 |
| D5 | 搜索策略 | AnySearch 走 MCP，默认开启，来源分级 + 出处追溯；可进正文与评分依据；配额：读 `.env` 的 Key（1000 次/天），无 Key 降级匿名（约 50 次/天） |
| D6 | 多人编辑 | 暂不支持，项目级编辑锁兜底，预留扩展字段 |
| D7 | 在线编辑边界 | 前端编辑结构化模型；后端管内容 CRUD、版本、AI 修改接口、导出转换 |
| D8 | 进度展示 | SSE/WebSocket 原样透传 Hermes 回复消息，不做事件转义 |
| D9 | 文档解析选型 | 文字版文档走 SDK（PyMuPDF/python-docx/openpyxl/OFDRW）直接提取；无文本层内容（扫描版 PDF、图片、内嵌图）走 Hermes `vision_model`（qwen-vl 云 API），不引入传统 OCR |
| D10 | 报价展示口径 | 产品 UI 同时展示"测算价"与"AI 建议价"两个口径，供用户对照确认 |

---

## 6. 非功能需求

- **性能**：标书生成为长任务（分钟级），异步队列 + 进度回报；任务失败可重试、可恢复
- **安全**：文件上传防恶意（zip bomb、病毒扫描）；企业数据硬隔离；报价写入门户需审计
- **可追溯**：全链路版本 + 审计 + 引用来源，是"可解释"的技术底座
- **可测试性**：报价算法、规则引擎为纯函数/确定性模块，单元测试覆盖

---

## 7. 待确认问题（与产品对齐）

1. AnySearch 匿名模式（无 Key）的实际限额：暂按约 50 次/天降级处理，上线前实测校准
2. 电力行业投标数据的数据安全要求：是否允许调用云端 VL/LLM API，还是要求私有化部署？（默认走云 API）

> 已确认项：OFD 用开源 OFDRW；Hermes 为开源 Agent，部署合规；AnySearch 注册 Key 每日 1000 次免费额度，从 Hermes `.env` 读取；成果导出可编辑格式（DOCX/XLSX）与成品格式（PDF）均支持；报价展示同时提供"测算价 + AI 建议价"两个口径。

---

## 8. 实施路线（里程碑，不含时间估计）

| 里程碑 | 内容 |
|---|---|
| M1 基础平台 | 认证/企业隔离、项目 CRUD、文件上传解包、基础存储 |
| M2 文件与解析 | 解析 SDK 接入、坐标体系、OFD 方案落地 |
| M3 Hermes 接入 | 部署、Profile/Session 隔离、任务编排、SSE 消息透传、基础 Skill |
| M4 成果闭环 | 结构化模型、版本链、在线编辑后端接口、生成/校核、模拟评标 |
| M5 报价与搜索 | 报价算法服务、AI 分层建议、AnySearch 接入与来源分级 |
| M6 导出与打磨 | 终稿检查、导出转换、审计完善、非功能达标 |

---

## 9. 细化计划（逐块推进）

按 4.1 ~ 4.10 逐模块细化，每个模块输出：接口契约（API 定义）、数据表 DDL、核心流程时序、依赖与风险。建议细化顺序：M1 相关模块（4.1 认证隔离、4.2 文件服务）→ 4.3 解析坐标 → 4.4 Hermes → 4.5/4.6 成果版本 → 4.7 报价 → 4.8/4.9 评标搜索 → 4.10 导出。
