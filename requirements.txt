# BidVolt 后端 Python 依赖（容器就绪后 pip install -r requirements.txt，再 pip freeze 固化版本）

# ===== 后端框架 =====
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.0
python-multipart>=0.0.9

# ===== 数据 =====
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2.1

# ===== 认证/安全 =====
python-jose[cryptography]>=3.3
bcrypt>=4.1
cryptography>=42.0

# ===== 文档解析 =====
pymupdf>=1.24            # 文本层 PDF
python-docx>=1.1
openpyxl>=3.1
easyofd>=2026.04         # OFD→图片转换（文本层解析为内置 zip/XML 实现，无需该库）
pyzipper>=0.3.6          # 压缩包（zip 炸弹防护逻辑配合使用）
patool>=1.12             # rar/7z 解包（依赖系统 unrar/7z 命令）

# ===== MCP / 外部调用 =====
mcp>=1.0                 # Hermes MCP server 框架（按实际 Hermes 版本匹配）
httpx>=0.27
clamd>=1.0.2             # ClamAV 客户端（unix socket 扫描）
